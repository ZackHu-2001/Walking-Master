import { View, Text, StyleSheet } from "react-native";

const AddRoom = () => {
  return (
    <View>
      <Text>Add Room</Text>
    </View>
  );
}

const styles = StyleSheet.create({

});

export default AddRoom;
